<?xml version="1.0" encoding="UTF-8"?>
<assembly>
  <id>bin</id>
  <!-- 打包的文件格式:tar.zip war zip -->
  <formats>
    <format>zip</format>
    <format>tar.gz</format>
  </formats>
<!-- <includeBaseDirectory>false</includeBaseDirectory> -->
  <includeBaseDirectory>true</includeBaseDirectory>



  <fileSets>
    <fileSet>
      <includes>
        <include>README.txt</include>
        <include>LICENSE.*.txt</include>
      </includes>
    </fileSet>

    <fileSet>
      <!-- Include license and notices generated by the remote resources plugin -->
      <!-- Failed to execute goal org.apache.maven.plugins:maven-assembly-plugin:2.4:attached (bin) on project ros-report: Failed to create assembly: Error creating assembly archive bin: You must set at least one file. -->
      <!-- <directory>target/maven-shared-archive-resources/META-INF</directory> -->
      <!-- 使用target路径，不会报错。 -->
      <!-- 打包路径 -->
      <!-- <directory>${project.basedir}/target/classes</directory> -->
      <directory>target/classes</directory>
      <!-- 打包后输出路径 -->
      <outputDirectory>/</outputDirectory>
      <!-- 过滤 ：打包需要包含的文件-->
      <includes>
        <include>**/*</include>
      </includes>
    </fileSet>        
    
    <!-- <fileSet>
        <directory>src/main/bin</directory>
        <outputDirectory>/bin</outputDirectory>
    </fileSet> -->     
     
    <fileSet>
      <directory>target/docs</directory>
      <outputDirectory>/docs</outputDirectory>
    </fileSet>
    
    <fileSet>
      <directory>target/api</directory>
      <outputDirectory>/javadocs</outputDirectory>
      <includes>
        <include>**/*</include>
      </includes>
    </fileSet>
    
  </fileSets>
  
  
  	<dependencySets>
		<dependencySet>
			<!-- 当前项目构件是否包含在这个依赖集合里 -->
<!--        <useTransitiveDependencies>true</useTransitiveDependencies> -->
<!--        <unpack>false</unpack> -->
			<useProjectArtifact>false</useProjectArtifact>
			<outputDirectory>lib</outputDirectory><!-- 将scope为runtime的依赖包打包到lib目录下。 -->
			<scope>runtime</scope>
           <!--  <excludes>
              <exclude>ros-report*.jar</exclude>
            </excludes> -->
          </dependencySet>
	</dependencySets>
</assembly>
